// Jest setup file referenced by jest.config.js (setupFiles).
// The Medusa starter template ships the config expecting this file; it was
// missing until the first test suite was added (rbac-guard unit tests).
// Intentionally empty — unit tests need no global setup.
